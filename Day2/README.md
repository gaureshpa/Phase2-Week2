# Support Ticket Database: Day 7

## Updates
+ Added `priority` attribute to `tickets` table with `low`, `medium` and `high` values.
+ Added `user_id` to `comments` to connect comments with users.
+ Populated all the tables in the database with seed data.
+ Tested the database with valid and invalid queries.
+ Updated the ER Diagram as per the update.

## Constraint Testing

Tested database rules for:
+ Invalid priority
+ Duplicate user email
+ Invalid foreign keys
+ Missing required values
